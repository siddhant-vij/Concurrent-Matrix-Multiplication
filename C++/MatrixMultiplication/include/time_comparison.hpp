#ifndef TIME_COMPARISON_HPP
#define TIME_COMPARISON_HPP

#include <vector>

class TimeComparison
{
public:
  static void compare(const std::vector<std::vector<int>> &matrix1, const std::vector<std::vector<int>> &matrix2);
};

#endif // TIME_COMPARISON_HPP
