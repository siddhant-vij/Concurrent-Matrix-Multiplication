#ifndef SEQUENTIAL_MULTIPLY_HPP
#define SEQUENTIAL_MULTIPLY_HPP

#include <vector>

class SequentialMultiply
{
public:
  static std::vector<std::vector<int>> multiply(const std::vector<std::vector<int>> &matrix1, const std::vector<std::vector<int>> &matrix2);
};

#endif // SEQUENTIAL_MULTIPLY_HPP
